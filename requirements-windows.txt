# Windows requirements for MDLM
# Python 3.9+ required

# IMPORTANT: Install PyTorch FIRST with CUDA 12.6
# pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu126

# ============================================================================
# Core dependencies (should install without issues)
# ============================================================================
datasets==2.18.0
einops==0.7.0
fsspec==2024.2.0
git-lfs==1.6
h5py==3.10.0
hydra-core==1.3.2
ipdb==0.13.13
lightning==2.2.1
notebook==7.1.1
jupyter==1.0.0
nvitop==1.3.2
omegaconf==2.3.0
packaging==23.2
pandas==2.2.1
rich==13.7.1
seaborn==0.13.2
scikit-learn==1.4.0
timm==0.9.16
transformers==4.38.2
wandb==0.13.5

# ============================================================================
# Triton for Windows (replaces triton==2.2.0)
# ============================================================================
triton-windows

# ============================================================================
# OPTIONAL: Packages below may fail to build on Windows
# Comment out if you encounter compilation errors
# ============================================================================

# ⚠️ causal-conv1d: Often fails on Windows due to missing source files
# Only needed for Mamba architecture (backbone=mamba)
# If using DiT architecture (backbone=dit or backbone=hf_dit), you can skip this
# causal-conv1d==1.1.3.post1

# ⚠️ mamba-ssm: Depends on causal-conv1d, will fail if above package fails
# Only needed for Mamba architecture (backbone=mamba)
# mamba-ssm==1.1.4

# ⚠️ flash-attn: Often fails on Windows, needs complex build setup
# Optional optimization, project works without it (just slower)
# flash-attn==2.5.6
